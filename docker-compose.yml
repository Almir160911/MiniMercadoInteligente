version: "3.9"
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: minimercado
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
volumes: {}
